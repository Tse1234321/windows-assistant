'use strict';

const {
  CATEGORIES,
  DEFAULT_KEYWORDS,
  buildScreenshotTargetPath,
  classifyScreenshot,
  getKeywordMap,
  getScreenshotsPath,
  getSettings,
  organizeScreenshots,
  scan,
  scanScreenshots,
} = require('./screenshotOrganizer');

module.exports = {
  CATEGORIES,
  DEFAULT_KEYWORDS,
  buildScreenshotTargetPath,
  classifyScreenshot,
  getKeywordMap,
  getScreenshotsPath,
  getSettings,
  organizeScreenshots,
  scan,
  scanScreenshots,
};
